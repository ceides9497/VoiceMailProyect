
public interface Subject {
	
	public void addOberserver(/*Observer observer*/);// por el momento como la clase esta implementando coquito no la tenemos
	public void deleteObserver(/*Observer observer*/);//same reason
	public void notificar();
}
